name: pocket-ic-stop
description: Kill pocket-ic by PID
inputs:
  pid:
    required: true
runs:
  using: composite
  steps:
    - shell: bash
      run: |
        set -euo pipefail
        run: |
          [ -f ./ic-bin/pocket-ic.pid ] && kill "$(cat ./ic-bin/pocket-ic.pid)" || true
