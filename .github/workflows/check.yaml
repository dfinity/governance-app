name: Check

on:
  workflow_call:

jobs:
  codestyle:
    name: Codestyle
    runs-on: ubuntu-latest
    steps:
      - name: Skip for now (debugging e2e)
        run: echo "Skipping codestyle checks"

  build_stats:
    name: Build Stats
    runs-on: ubuntu-latest
    steps:
      - name: Skip for now (debugging e2e)
        run: echo "Skipping build stats"

  unit_tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Skip for now (debugging e2e)
        run: echo "Skipping unit tests"

  e2e_tests:
    name: E2E
    uses: ./.github/workflows/e2e.yml
